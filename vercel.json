{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 15
    }
  },
  "rewrites": [
    {
      "source": "/api/webhook/singpay/:token",
      "destination": "/api/webhook/singpay?token=:token"
    }
  ]
}
